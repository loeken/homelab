{{ if .Values.ha.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: home-assistant
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "9"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    chart: home-assistant
    repoURL: https://loeken.github.io/helm-charts
    targetRevision: {{ .Values.ha.chartVersion }}
    helm:
      values: |
        controller:
          type: statefulset
        persistence:
          config:
            enabled: true
            accessMode: ReadWriteOnce
            storageClass: {{ .Values.storageClass }}
            size: 4Gi
            readOnly: false
        ingress:
          main:
            annotations:
              kubernetes.io/ingress.class: "nginx"
              cert-manager.io/cluster-issuer: "{{ .Values.clusterIssuer }}"
              externaldns.nginx.org/hostname: "ha.{{ .Values.domain }}."
              externaldns.nginx.org/cloudflare-proxied: "false"
              externaldns.nginx.org/ttl: "120"
              {{- if .Values.ha.useAuthelia }}
              nginx.ingress.kubernetes.io/auth-url: "http://authelia.authelia.svc.cluster.local/api/verify"
              nginx.ingress.kubernetes.io/auth-signin: "https://auth.{{ .Values.domain }}"
              {{end}}
            enabled: {{ .Values.ha.ingress.nginx.enabled }}
            hosts:
              - host: ha.{{ .Values.domain }}
                paths:
                  - path: /
                    pathType: Prefix
                    service:
                      port: 8123
            tls:
            - secretName: ha-tls
              hosts:
                - ha.{{ .Values.domain }}

        postgresql:
          enabled: true
          global:
            postgresql:
              auth:
                database: home-assistant
                existingSecret: home-assistant
          primary:
            persistence:
              enabled: true
              storageClass: {{ .Values.storageClass }}
              accessModes:
                - ReadWriteOnce
        service:
          main:
            ports:
              http:
                port: 8123
  destination:
    namespace: home-assistant
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
{{ end }}
